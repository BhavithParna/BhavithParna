name: generate snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: generate snake.svg
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          # Optional: set a GH_PAT secret (classic token, `repo` scope) so the
          # contribution calendar includes private-repo commits. Falls back to
          # the action token (public contributions only) when the secret is unset.
          github_token: ${{ secrets.GH_PAT || github.token }}
          outputs: |
            dist/snake-dark.svg?color_snake=#F5EBE0&color_dots=#2A1C12,#5C3D26,#8B5A2B,#C08552,#DDB892
            dist/snake.svg?color_snake=#5C3D26&color_dots=#EFE4D6,#E6CCB2,#D4A373,#B07A45,#7A4A28

      - name: push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
